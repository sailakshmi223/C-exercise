#include <stdio.h>

int main()
{
    char s[100];
    scanf("%s",s);
    int i;
    for(i=0;s[i]!='\0';i++){
      s[i]-=32;  //difference between uppercase and lower case in ASCII is 32
    }
    printf("%s",s);
    

    return 0;
}
